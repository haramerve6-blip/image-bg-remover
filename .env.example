# remove.bg API key (get from https://www.remove.bg/api)
REMOVE_BG_API_KEY=your-remove-bg-api-key

# Optional: if deploying API to a separate endpoint
# NEXT_PUBLIC_API_URL=https://your-worker-url.workers.dev
